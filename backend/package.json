{
  "name": "dataverse-news-backend",
  "engines": { "node": ">=18" },
  "version": "1.0.0",
  "description": "Express + SQLite backend for Dataverse News. Runs scheduled API fetches and serves articles to the React frontend.",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "seed": "node src/startup.js"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "sql.js": "^1.12.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
